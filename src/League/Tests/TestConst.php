<?php

    namespace League\Tests;

    class TestConst {
        const FIRST_ROUND = 1;
        const SECOND_ROUND = 2;

        const ZERO_MATCHES = 0;
        const ONE_MATCH = 1;
        const TWO_MATCHES = 2;

        const ZERO_GOALS = 0;
        const ONE_GOAL = 1;
        const TWO_GOALS = 2;
        const THREE_GOALS = 3;

        const THREE_POINTS = 3;
        const SIX_POINTS = 6;
    }